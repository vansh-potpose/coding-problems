/**
 * @param {number} n
 * @return {Function} counter
 */
var createCounter = function(n) {
    this.k=-1;
    return function() {
        k=k+1;
        return n+k;
    };
};

/** 
 * const counter = createCounter(10)
 * counter() // 10
 * counter() // 11
 * counter() // 12
 */
