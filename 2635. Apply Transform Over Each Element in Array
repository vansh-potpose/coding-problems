/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function(arr, fn) {
    let myArray = new Array();
    for(let i = 0; i < arr.length; i++){
        myArray.push(fn(arr[i],i));
    }
    return myArray;
};
