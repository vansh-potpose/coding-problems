class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int aws=0;
        int n=nums.size();
        for(int i=0;i<n;i++){
            aws=aws^nums[i];
        }
        return aws;
    }
};
